<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.sakana.order.mapper.AddressMapper">

    <insert id="insert">
        insert into orders.order_address
            (id, order_id, receiver, phone, province, city, district, detail, created_time)
        values
            (#{id}, #{orderId}, #{receiver}, #{phone}, #{province}, #{city}, #{district}, #{detail}, #{createdTime})
    </insert>

</mapper>
